# Project Instructions

- Do not start local dev servers unless the user explicitly asks for it in the current turn.
- Do not install dependencies unless the user explicitly asks for it in the current turn.
- For UI work, design for mobile-app interaction first. Avoid dense desktop-style pages and avoid putting every feature on one screen.
- Prefer a simple home entry screen that routes users into focused feature pages when the product has multiple major modes.
